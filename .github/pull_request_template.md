## Summary

<!-- What does this PR do? -->

## Checklist

- [ ] Tested locally
- [ ] `npm run all` passes (prettier, lint, tests, code coverage badge generation)
- [ ] `action.yml` updated (if inputs/outputs changed)
- [ ] Updated version in `package.json` and `package-lock.json` (`npm version patch --no-git-tag-version` can help)

## Related Issues

<!-- Fixes #123 -->
